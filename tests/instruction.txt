You can run them using the command `java -jar /path/to/munit.jar tests/Hw3Test*.class hw3.asm`. If needed compile the java files using the command `javac -cp /path/tomunit.jar tests/Hw3Test*.java file`. Replace the * with the test number in the commands. Few
